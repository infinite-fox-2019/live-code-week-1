function generateSnake(row, column) {
  //your code here
  var result = '';
  var jml = column * column;
  for (let i = 0; i < row; i++) {
      console.log(result);
      console.log('\n')l
  }
};

(generateSnake(3, 6);
